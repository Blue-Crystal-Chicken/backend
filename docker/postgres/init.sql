-- Nessuna inizializzazione extra necessaria.
-- Il database principale (blue_crystal_db) viene creato da POSTGRES_DB.
-- Il servizio notifiche usa lo STESSO database in uno schema dedicato ('notif'),
-- creato automaticamente da Hibernate: non serve più un secondo database.
SELECT 1;
